"""STUB: 部署触发接口定义 — MVP 阶段不实现"""


async def trigger_deploy(task_id: int) -> dict:
    return {
        "success": False,
        "data": None,
        "message": "Deploy not implemented in MVP. See Project.deploy_provider for planned provider.",
    }
