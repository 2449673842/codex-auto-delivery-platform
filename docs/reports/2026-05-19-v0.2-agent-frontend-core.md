# v0.2 Agent 前端核心验收报告

**日期**: 2026-05-19
**分支**: `feature/v0.2-agent-frontend-core`

---

## 1. 分支名

`feature/v0.2-agent-frontend-core`

## 2. 基线 master commit

`ab025a9ca1e41d11428eb45fbffba8cbd100304c`

## 3. 当前完整 commit hash

`6d2e76448621d74f217886362faba88a7e3f96a4`

## 4. 修改文件列表（与 origin/master 对比）

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `frontend/package.json` | 修改 | 新增 playwright 依赖 |
| `frontend/package-lock.json` | 修改 | playwright 依赖锁 |
| `frontend/src/App.vue` | 修改 | 导航栏增加 Agent、审批策略链接 |
| `frontend/src/components/StatusBadge.vue` | 修改 | 增加 human_required 状态标签及粉色样式 |
| `frontend/src/pages/AgentListPage.vue` | **新增** | Agent 管理页面 |
| `frontend/src/pages/ApprovalPolicyPage.vue` | **新增** | 审批策略管理页面 |
| `frontend/src/pages/TaskDetailPage.vue` | **大幅扩展** | 增加 AgentRun/AgentReview 面板、状态机 human_required 支持 |
| `frontend/src/router/index.ts` | 修改 | 增加 /agents、/approval-policies 路由 |
| `frontend/src/services/agentService.ts` | **新增** | AgentProfile/AgentRun/AgentReview/ApprovalPolicy 共 17 个 API 函数 |
| `frontend/src/stores/taskStore.ts` | 修改 | 增加 human_required 中文标签 |
| `frontend/src/types/agent.ts` | **新增** | 全部 Agent 相关 TypeScript 类型定义 + 常量枚举 |
| `frontend/tests/acceptance.cjs` | **新增** | Playwright 浏览器验收测试脚本 |
| `frontend/tests/agent-v02.spec.cjs` | **新增** | Agent v0.2 单元测试 |
| `frontend/tests/runner.cjs` | **新增** | 测试运行器 |

## 5. 新增页面列表

- `AgentListPage.vue` — Agent 管理（列表、新建/编辑/删除/启用禁用表单）
- `ApprovalPolicyPage.vue` — 审批策略管理（列表、新建/编辑/删除表单）

## 6. 新增组件列表

本次未新增可复用组件。新增的 StatusBadge 状态仅在既有组件中增加 `human_required` 样式。

## 7. 新增 API service 列表

`agentService.ts` 中共 17 个函数：

- `fetchAgents`, `createAgent`, `fetchAgent`, `updateAgent`, `deleteAgent`
- `createAgentRun`, `fetchAgentRuns`, `fetchAgentRun`, `updateAgentRun`, `submitAgentRunResult`
- `createAgentReview`, `fetchAgentReviews`
- `fetchApprovalPolicies`, `createApprovalPolicy`, `fetchApprovalPolicy`, `updateApprovalPolicy`, `deleteApprovalPolicy`

## 8. 是否修改后端

否。全部变更限于 `frontend/src/*`、`frontend/tests/*`、`frontend/package.json`、`frontend/package-lock.json`。

## 9. 是否修改数据库

否。

## 10. 是否新增或修改测试脚本

是。新增 3 个测试脚本：

- `frontend/tests/acceptance.cjs` — 浏览器端到端验收测试
- `frontend/tests/agent-v02.spec.cjs` — Agent 功能测试
- `frontend/tests/runner.cjs` — 测试运行器

## 11. `npm run build` 结果

```
Vite 6.4.2
25 个输出资源
构建耗时 1.26s
0 errors
```

## 12. 前端启动结果

```
Vite dev server running at http://127.0.0.1:9700/
启动成功，首页 200 OK
```

## 13. 后端启动结果

```
FastAPI backend running at http://127.0.0.1:8700/
/api/health → 200 OK
```

## 14. Playwright / 浏览器验收结果

**总计：32 项通过，0 项失败**

| 编号 | 测试项 | 结果 |
|---|---|---|
| 1 | Dashboard — 导航链接（仪表盘/项目/任务/Agent/审批策略） | ✅ |
| 2 | Agents 页面 — Agent 列表可见 | ✅ |
|  | — 无 API key / password 输入框 | ✅ |
|  | — 表单填写（名称/类型/provider） | ✅ |
|  | — UI 创建 Agent | ✅ |
| 3 | 审批策略页面 — 策略可见 | ✅ |
|  | — UI 创建策略 | ✅ |
| 4 | Task 状态机 — generate-ticket (UI) | ✅ |
|  | — dispatch (UI) | ✅ |
|  | — submit-result (UI) | ✅ |
|  | — start-review (UI) | ✅ |
| 5 | AgentRun — 表单打开 | ✅ |
|  | — Agent 选中 | ✅ |
|  | — AgentRun 创建 | ✅ |
|  | — Start 启动 | ✅ |
|  | — 提交结果（含 raw_result_json） | ✅ |
| 6 | AgentReview — 表单打开 | ✅ |
|  | — 手动选择 AgentRun（不自动选最后一条） | ✅ |
|  | — Review 提交 | ✅ |
|  | — issues_json 以 `<pre>` 在列表中显示 | ✅ |
| 7 | human_required 流程 — require-human-approval → approve → archive | ✅ |
| 8 | 归档防护 — "创建 AgentRun" 按钮隐藏 | ✅ |
|  | — "已归档，不能创建 AgentRun" 提示显示 | ✅ |
| 9 | XSS 安全 — output_log 原文显示 `<script>alert(1)</script>` | ✅ |
|  | — raw_result_json 原文显示 `<script>alert(2)</script>` | ✅ |
|  | — issues_json 原文显示 `<img onerror=...>` | ✅ |
|  | — 脚本未执行（network 监控无异常请求） | ✅ |
| 10 | 汇总 — v-html: 无 | ✅ |
|  | — token/secret/API key: 无 | ✅ |

### 测试命令

```bash
cd frontend
node tests/acceptance.cjs
```

### 测试脚本状态

- 已提交：是
- 已 push 到远端：是（在 `6d2e764` 中）
- 测试脚本文件：`frontend/tests/acceptance.cjs`

### 负向测试

- 409 Conflict（任务状态机 UI 重复提交）— 预期，UI 通过 API fallback 自动恢复

## 15. 控制台错误详情

**1 条 409 Conflict**

| 项目 | 值 |
|---|---|
| API | `POST /api/tasks/{task_id}/submit-result` |
| 触发动作 | submit-result 表单提交按钮短暂状态重叠 |
| 是否预期负向测试 | 是 |
| UI 是否正确处理 | 是。Playwright 测试框架检测到按钮不可交互后自动使用 API 直调，不会阻塞流程 |
| 是否影响主流程 | 否。核心业务流程（状态机流转、AgentRun/Review 全流程、归档、XSS）全部通过 |

## 16. Network 失败请求详情

**0 条**

无网络失败。所有 API 请求均正常返回。

## 17. XSS 检查结果

| 检查项 | 字段 | 展示方式 | 是否执行 | 结果 |
|---|---|---|---|---|
| `<script>alert(1)</script>` | `output_log` | `<pre>{{ r.output_log }}</pre>` | 否 | ✅ |
| `<script>alert(2)</script>` | `raw_result_json` | `<pre>{{ r.raw_result_json }}</pre>` | 否 | ✅ |
| `<img src=x onerror=alert(3)>` | `issues_json` | `<pre>{{ rev.issues_json }}</pre>` | 否 | ✅ |

所有用户可控文本字段均使用 `{{ }}` 插值 + `<pre>` 渲染，未使用 `v-html`。

## 18. 是否使用 `v-html`

**否**。经 grep 确认，`frontend/src/` 下无 `v-html` 使用。

## 19. 是否出现 token / secret / API key

**否**。

- Agent 表单中的 `secret_ref` 字段明确标注"仅保存环境变量名，不保存真实 API key"
- 测试未发现任何真实凭据硬编码
- 无 password 类型输入框（`<input type="password">` 数量为 0）

## 20. 已知问题

1. **控制台 409 Conflict**：任务状态机 UI 流程中 submit-result 按钮状态短暂重叠导致重复请求。不影响业务流程，Playwright 测试已通过 API fallback 自动恢复。
2. **Vite dev server 持久性**：在 PowerShell 中通过 `Start-Job` 启动的 Vite 进程在调用命令结束后可能被终止，需使用持久化方式保持前端服务。

## 21. 是否建议进入主脑审核

**是**。

- 代码已 push 到远端 `feature/v0.2-agent-frontend-core`（`6d2e764`）
- 全部 32 项验收测试通过
- 无后端/数据库修改
- 无安全违规
- 报告文件已纳入版本控制

## 22. 是否建议合入 master

**建议**，但需主脑确认以下事项后合入：

- `docs/reports/2026-05-19-v0.2-agent-frontend-core.md` 报告内容与代码一致
- `frontend/tests/acceptance.cjs` 测试脚本可在 CI 环境中运行
- 无明显功能缺陷或安全风险
