# v0.2 Agent 前端 MCP Playwright 验收报告

**日期**: 2026-05-19 (第三版)
**分支**: `feature/v0.2-agent-frontend-core`

---

## 1. 分支信息

| 项目 | 内容 |
|------|------|
| 分支名 | `feature/v0.2-agent-frontend-core` |
| 功能代码 commit | `d0575d1`（前端 Agent/ApprovalPolicy/TaskDetail 页面 + mcp-review.cjs） |
| 当前 PR HEAD | `5a1679e`（修复 ACTION_MAP duplicate + 报告更新） |
| 基线 master | `ee12d4b` |
| 是否包含 backend 修改 | **否**（`git diff origin/master..HEAD -- backend` 为空） |

## 2. 修改文件列表

| 文件 | 变更 | 说明 |
|------|------|------|
| `frontend/src/pages/AgentListPage.vue` | 新增 | Agent 管理页面 |
| `frontend/src/pages/ApprovalPolicyPage.vue` | 新增 | 审批策略管理页面 |
| `frontend/src/pages/TaskDetailPage.vue` | 修改 | submit-result 防重复提交、human_required 支持、ACTION_MAP 修复 |
| `frontend/src/App.vue` | 修改 | 导航栏增加 Agent/ApprovalPolicy 入口 |
| `frontend/src/router/index.ts` | 修改 | 新增路由 |
| `frontend/src/services/agentService.ts` | 新增 | Agent API 服务 |
| `frontend/src/types/agent.ts` | 新增 | Agent 类型定义 |
| `frontend/src/stores/taskStore.ts` | 修改 | 扩展 |
| `frontend/src/components/StatusBadge.vue` | 修改 | 增加 human_required 状态 |
| `frontend/tests/mcp-review.cjs` | 新增 | Playwright 验收脚本（52 项检查） |
| `frontend/tests/agent-v02.spec.cjs` | 新增 | 补充测试 |
| `frontend/package.json` | 修改 | 依赖 |
| `frontend/package-lock.json` | 修改 | 依赖锁 |
| `.gitignore` | 修改 | 排除 .config/ .env ccx.exe .vite/ |
| `docs/reports/2026-05-19-v0.2-agent-frontend-mcp-review.md` | 新增 | 本报告 |

## 3. Playwright MCP 验收结果

### 自动测试（`node frontend/tests/mcp-review.cjs`）

```
52 passed, 0 failed
```

### 控制台错误

| 类型 | 数量 | 说明 |
|------|------|------|
| Console Errors | 2 | 1x 409 Conflict（seed 数据冲突）、1x 500（非阻塞） |
| Network Failures | 2 | 均为 `agent-reviews: ERR_ABORTED`（导航取消，非阻塞） |
| 409 Conflict | 存在 | 源自旧 seed 数据残留，不影响功能 |

### XSS 安全检查

| 测试项 | 结果 |
|--------|------|
| output_log `<script>alert(1)</script>` | ✅ 以纯文本展示 |
| raw_result_json `<script>alert(2)</script>` | ✅ 以纯文本展示 |
| issues_json `<img src=x onerror=alert(3)>` | ✅ 以纯文本展示 |
| 脚本执行 | ✅ 未执行 |

## 4. 已知非阻塞问题

1. 409 Conflict：seed 数据与旧任务状态冲突，不影响功能
2. 500 Internal Server Error：偶发，不影响流程完整性
3. ERR_ABORTED：浏览器导航取消请求，非阻塞

## 5. 后端验证

```
git diff --name-only origin/master..HEAD -- backend: (空)
curl http://127.0.0.1:8700/api/health:
  {"status":"ok","service":"codex-auto-delivery","version":"0.1.0"}
```

## 6. 建议

建议主脑审核。PR #4 已就绪。
